/* table */
const DEFAULT_SORT_FIELD = 'name';
const EVENT_TABLE_FIELDS = [
  { label: 'Title', value: 'title', sortable: true },
  { label: 'Date', value: 'date', sortable: true }
];

// file management
const MAX_IMAGES_ACCEPTED = 1;
const MAX_SIZE_IMAGE = 1048576; // 1MB

export { DEFAULT_SORT_FIELD, EVENT_TABLE_FIELDS, MAX_IMAGES_ACCEPTED, MAX_SIZE_IMAGE };

